import { createTheme } from "@mui/material";

const fontTheme = createTheme({
	typography: {
		fontFamily: "Poppins",
		fontWeight: "bold",
		fontSize: "1.5rem",
	},
	palette: {
		color: "pink",
	},
});

export default fontTheme;
